services:
  demo-smart-contract:
    image: ${CLIENT_IMAGE_TAG:-integritee-cli:dev}
    container_name: integritee-smart-contract-demo-${NETWORK:-default}
    devices:
      - "/dev/sgx/provision:/dev/sgx/provision"
      - "/dev/sgx/enclave:/dev/sgx/enclave"
    volumes:
      - "/var/run/aesmd:/var/run/aesmd"
    build:
      context: ..
      dockerfile: build.Dockerfile
      target: deployed-client
    depends_on:
      integritee-node-${NETWORK:-default}:
        condition: service_healthy
      integritee-worker-1-${NETWORK:-default}:
        condition: service_healthy
      integritee-worker-2-${NETWORK:-default}:
        condition: service_healthy
    environment:
      - RUST_LOG=warn,ws=warn,itc_rpc_client=warn
    networks:
      - integritee-test-network-${NETWORK:-default}
    entrypoint:
      "/usr/local/worker-cli/demo_smart_contract.sh -p 9912 -u ws://integritee-node-${NETWORK:-default}
      -V wss://integritee-worker-1-${NETWORK:-default} -A 2011 -C /usr/local/bin/integritee-cli 2>&1"
    restart: "no"
